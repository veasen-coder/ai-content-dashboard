import { WorkspaceSettings } from "@/components/settings/workspace-settings";

export const metadata = { title: "Settings — Flogen AI" };

export default function SettingsPage() {
  return <WorkspaceSettings />;
}
