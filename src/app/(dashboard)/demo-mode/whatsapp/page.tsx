"use client";

// Demo mode WhatsApp page re-uses the real WhatsApp CRM so clients can see
// the actual chatbot working live during sales demos.
import WhatsAppCrmPage from "@/app/(dashboard)/whatsapp/page";

export default function DemoWhatsAppPage() {
  return <WhatsAppCrmPage />;
}
